import React from 'react';
import { Link } from 'react-router-dom';

const Dashboard = () => (
    <div>
        <p>Dashboard has nothing here at the moment. <Link to="/browse"> Looking to browse? </Link></p>
    </div>
);

export default Dashboard;
